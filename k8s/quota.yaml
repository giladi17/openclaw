apiVersion: v1
kind: ResourceQuota
metadata:
  name: openclaw-quota
  namespace: default
spec:
  hard:
    count/jobs.batch: "10"
    pods: "20"
    requests.memory: "2Gi"
    requests.cpu: "2"
    limits.memory: "4Gi"
    limits.cpu: "4"